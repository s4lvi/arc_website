import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ARC | Agricultural Research Corporation",
  description: "Pioneering automation research and development for small to medium sized farms, greenhouses, and consumer garden systems. Based in Saginaw, Michigan.",
  keywords: ["agricultural automation", "farm technology", "greenhouse automation", "smart farming", "agricultural research", "Saginaw Michigan"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
